[project]
name = "pack-qa-checker"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "Jinja2>=3.1,<4",
    "pydantic>=2.13,<3",
    "PyYAML>=6.0,<7",
]

[project.optional-dependencies]
test = ["pytest>=8,<10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
